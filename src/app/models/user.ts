export interface User {
    id: number;
    userName: string;
    password: string;
    type:string;

  
  /*  constructor(id: number, userName: string, password: string, enabled: boolean, passwordLastUpdate: Date) {
      this.id = id;
      this.userName = userName;
      this.password = password;
      this.enabled = enabled;
      this.passwordLastUpdate = passwordLastUpdate;
    }*/
  }